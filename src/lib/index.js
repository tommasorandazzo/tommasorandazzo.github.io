// place files you want to import through the `$lib` alias in this folder.
import createHttpHeaders from './createHttpHeaders';
import fetchJson from './fetchJson';

export default { createHttpHeaders, fetchJson };
